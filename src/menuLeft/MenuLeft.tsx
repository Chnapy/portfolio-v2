import React from 'react';

export interface MenuLeftProps {
    
}

export interface MenuLeftState {
    
}

export default class MenuLeft extends React.Component<MenuLeftProps, MenuLeftState> {

    constructor(props: MenuLeftProps) {
        super(props);

        this.state = {};
    }
    
    render() {

        return <div/>;
    }
}